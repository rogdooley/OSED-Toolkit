schema_version: 1

profile:
  type: located

produced_by:
  stage: address_validation

produced_from:
  - vulnapp2.candidate.yaml

provenance:
  candidate:
    path: vulnapp2.candidate.yaml
    sha256: null  # populated by validator at runtime

# ---------------------------------------------------------------------------
# Address validation result
# ---------------------------------------------------------------------------
# Answers one question: where are my controlled bytes at runtime?
# Nothing about byte values, transformations, or bad characters belongs here.

validated_copy_site:
  id: vulnapp2.copy.001

validated_expression:
  expr: poi(@esp+8)
  confidence_prior: 0.95
  note: Confirmed. Magic bytes observed at this expression when breakpoint fires.

prefix_length: 0

status: located
