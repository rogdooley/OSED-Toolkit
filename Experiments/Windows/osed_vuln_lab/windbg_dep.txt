OSED WinDbg Commands - dep Profile (VM-Only)
============================================

Target profile
- dep: /GS- /DYNAMICBASE:NO /NXCOMPAT

Launch
- windbg -g -G osed_vulnsvc.exe 9999

Setup
- .symfix
- .reload /f
- sxe av

Focus
- DEP-aware crash triage and ROP preparation for OP_ROP.

Commands
- !analyze -v
- r
- kb
- dd esp L60
- u eip-20 L40

Module review
- lm
- lmv m osed_vulnsvc
- lmv m osedhelper

Required PUSHAD-chain inventory
- pop edi; ret and a standalone ret
- pop esi; ret and a callable VirtualProtect wrapper/thunk
- pop ebp; ret and jmp esp
- pop eax; ret, neg eax; ret, and xchg eax,ebx; ret
- pop edx; ret and pop ecx; ret
- pushad; ret and a writable dword
- Record instruction bytes, absolute addresses, and owning module.

VirtualProtect verification
- bp kernel32!VirtualProtect
- g
- r
- dd esp L20
- g

Notes
- Populate gadgets/gadgets_template.json only from debugger-verified findings.
- Tools.rop.GadgetDB accepts the template's modules/gadgets envelope.
- Append a NOP sled and benign proof bytes immediately after the serialized
  PUSHAD chain.
