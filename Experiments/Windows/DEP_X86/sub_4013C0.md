
```asm
sub_4013C0 proc near

arg_0= dword ptr  8
arg_4= dword ptr  0Ch

push    ebp
mov     ebp, esp
push    0Ch             ; int
mov     eax, [ebp+arg_4]
push    eax             ; int
mov     ecx, [ebp+arg_0]
mov     edx, [ecx]
push    edx             ; s
call    recv_all
add     esp, 0Ch
cmp     eax, 0Ch
jz      short loc_4013E1
```

```asm
push 0Ch                    ; arg (12)
mov eax, [ebp+arg_4]        ; address inside function's stack frame eax = &(ebp+arg_4)
push eax                    ; another function argument
mov     ecx, [ebp+arg_0]    ; address inside function's stack frame ecx = &(ebp+arg_0)
mov     edx, [ecx]          ; edx = *(DWORD *)ecx;
push    edx                 ; another function arg
call recv_all               ; arguments look like (edx, eax, 0x0C)
add     esp, 0Ch            ; caller cleans arguments
cmp     eax, 0Ch            ; eax == 0x0C ?
jz      short loc_4013E1    ; if 0 then go to loc_4013E1
```


## Proven:

function:
```c
sub_401300(
    *(DWORD *)arg_0,
    arg_4,
    12
);
```

```c
if (bytesRead == 12)
{
    ...
}
```

## If bytesread == 12

```asm
loc_4013E1:
mov     eax, [ebp+arg_4]
cmp     dword ptr [eax], 53564C56h
jz      short loc_401409
```

If we just cleaned the stack frame, still the [epb+arg_4] still exists.

```C
eax = *(DWORD *)(epb+arg_4)
if (*(DWORD *)arg_4 == 0x53564C56)
if (*(DWORD *)arg_4 == MAGIC)
    goto success;
```

```text
53 = 'S'
56 = 'V'
4C = 'L'
56 = 'V'

VLVS
```

## If MAGIC

```asm
loc_401409:
mov     eax, [ebp+arg_4]
cmp     dword ptr [eax+8], 2000h
jbe     short loc_401446
```

eax = Memory32[ebp+arg_4]
compare Memory32[eax+8] with 2000h
Jump if Memory32[eax+8] <= 0x2000 (unsigned)


## If <= 2000h

```asm
loc_401446:
mov     ecx, [ebp+arg_0]
mov     edx, [ecx+3Ch]
add     edx, 0Ch
mov     eax, [ebp+arg_0]
mov     [eax+3Ch], edx
xor     eax, eax
```

ecx = Memory32[ebp+arg_0]
edx = Memory32[ecx+0x3c]
edx += 0x0c
eax = Memory32[ebp+arg_0]
Memory32[eax+0x3c] = edx + 0x0c
eax = 0x0

Load field at offset 0x3C
Increase it by 12
Store it back
Return 0

## Next

```asm
loc_401457:
pop     ebp
retn
sub_4013C0 endp
```

Restore caller's EBP.
Pop the return address into EIP.
Continue execution in the calling function.


```asm
sub_4014D0 proc near

var_208= dword ptr -208h
var_204= dword ptr -204h
var_8= dword ptr -8
var_4= dword ptr -4
arg_0= dword ptr  8
arg_4= dword ptr  0Ch
arg_8= dword ptr  10h

push    ebp
mov     ebp, esp
sub     esp, 208h
cmp     [ebp+arg_8], 0
jz      short loc_4014F3
```

A bunch of stack offsets
